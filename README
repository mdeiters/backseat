== FEATURES (TODO)
  * support 'display' option (a=always or p=until particpated)
  * add cas (SSO), user managed list  
  * skip_jquery_include
  
== DOCUMENTATION (TODO)
  #mode=image
  #mode=inline
  #user=matthew deiters (any way to identify a user)
  only support jquery