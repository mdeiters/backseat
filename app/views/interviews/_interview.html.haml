
.interview{:id => dom_id(interview)}
  .title
    =link_to interview.question, interview_path(interview)
  .options= interview.options.map(&:name).join(', ')
  .summary
    respones:
    = interview.responses.size