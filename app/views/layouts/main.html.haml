%html
  %head
    = stylesheet_link_tag 'main'
    = javascript_include_tag :defaults
  %body
    #wrapper
      #header
        #logo
          =image_tag 'logo_white.png'
        #tagline.left
          Get feedback from your users!  
        #subnav.right
          - if logged_in?
            =link_to 'My Interviews', interviews_url
            |
            =link_to 'Logout', session_url, :method => :delete
        .clear  
      #message=flash[:message] if flash[:message]
      #error=flash[:error] if flash[:error]
      #content
        #banner 
          =yield :banner
        #big_column
          =yield
        #small_column 
          =yield :small 
        .clear  
      #footer
        If you're enjoying this open source app, please recommend me at 
        =link_to 'WorkingWithRails', 'http://www.workingwithrails.com/person/8183-matthew-deiters'